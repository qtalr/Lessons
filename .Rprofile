dir.create(".Courses/", showWarnings = FALSE)
dir.create(".user_data/", showWarnings = FALSE)

options(
  browser = "open -a 'Zen Browser'",
  swirl_courses_dir = ".Courses/",
  swirl_data_dir = ".user_data/"
)
